import{Router}from'express';import{listCatalog}from'../services/catalog.js';export function createRouter(){const r=Router();r.get('/catalog',(_,res)=>res.json({catalog:listCatalog()}));r.get('/privacy',(_,res)=>res.json({sensitiveData:'encrypted, consent-based, retention-limited'}));r.get('/status',(_,res)=>res.json({service:'online',timezone:process.env.TZ||'Asia/Jakarta'}));return r;}